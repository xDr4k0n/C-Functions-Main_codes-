//---------------------------------------------------------------------------------
void Auto_Load(int array[],int dim);
//---------------------------------------------------------------------------------
void Auto_Load(int array[],int dim)
{
    //srand(time(0));
    //#include <time.h>
    printf("\n ---Loading---->\n");
    int i;
    for (i = 0; i < dim; i++)
    {

        //array[i] = (rand() % 50);
    }
}
